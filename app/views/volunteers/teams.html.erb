<div id="team-details" >
	<h1>Project Teams</h1>
	<h5> Here you can see which volunteers each group has.</h5>
	<br>
	<% @grouped.each do |group, volunteers| %>
  	<%= render 'groups', :locals => {:group => group, :volunteers => volunteers} %>
  <br> 
	<% end %>
</div>